$sprites: (
  {{#each layouts}}
  {{#each layout.items}}
    {{#if ../../opt.split}}{{cssesc ../../name}}-{{/if}}{{cssesc meta.name}}: -{{baseDim x}}px -{{baseDim y}}px {{baseDim width}}px {{baseDim height}}px{{#if @last}},{{/if}}
  {{/each}}
  {{/each}}
);