$sprites: (
  {{#each layouts}}
  {{#each layout.items}}
  {{#if ../../opt.split}}{{cssesc ../../name}}-{{/if}}{{cssesc meta.name}}: -{{baseDim x}}px -{{baseDim y}}px {{baseDim width}}px {{baseDim height}}px{{#unless @last}},{{/unless}}
  {{/each}}{{#unless @last}},{{/unless}}
  {{/each}}
);